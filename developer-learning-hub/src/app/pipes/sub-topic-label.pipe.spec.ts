import { SubTopicLabelPipe } from './sub-topic-label.pipe';

describe('SubTopicLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SubTopicLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
